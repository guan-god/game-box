import { ReactNode } from 'react';
export default function GameLayout({children,title,meta,onRestart,mode,setMode}:{children:ReactNode;title:string;meta:string;onRestart:()=>void;mode:'standard'|'endless';setMode:(m:'standard'|'endless')=>void}){return <div className='app'><header className='glass top'><h1>{title}</h1><p>{meta}</p><div className='row'><button className='btn subtle' onClick={()=>setMode('standard')}>标准模式</button><button className='btn subtle' onClick={()=>setMode('endless')}>无尽模式</button><button className='btn' onClick={onRestart}>新开一局</button></div></header>{children}</div>}
