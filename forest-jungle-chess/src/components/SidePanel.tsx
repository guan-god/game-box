import { CardType, GameState, Piece } from '../game/types';import MoveLog from './MoveLog';import CapturedPieces from './CapturedPieces';
const CN:Record<CardType,string>={shield:'光盾',stealth:'潜行',combo:'连击'};
export default function SidePanel({state,selected,onRestart,onUndo,onRules,onUseCard}:{state:GameState;selected?:Piece;onRestart:()=>void;onUndo:()=>void;onRules:()=>void;onUseCard:(card:CardType)=>void}){const hand=state.cards[state.turn];return <aside><div className='panel'><h3>当前回合: {state.turn==='red'?'红方':'蓝方'}</h3><button onClick={onRestart}>重新开始</button><button onClick={onUndo}>悔棋</button><button onClick={onRules}>规则</button><div className='extra'><b>手牌</b>{hand.length?hand.map((c,i)=><button key={`${c}${i}`} onClick={()=>onUseCard(c)} disabled={!selected}>使用{CN[c]}</button>):<div>无可用手牌</div>}<small>{selected?`目标：${selected.side==='red'?'红':'蓝'}方${selected.type}`:'先选中己方棋子再用牌'}</small></div></div><CapturedPieces red={state.captured.red} blue={state.captured.blue}/><MoveLog moves={state.moves}/></aside>}
